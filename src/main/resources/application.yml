spring:
  datasource:
    url: "jdbc:mysql://localhost/test-wolrd-mysql"
    username: "root"
    password: "mysql"
 
  sql: 
    init: 
        mode: always 
        encoding: UTF-8


logging:
  level:
    root: info
  file:
    name: app.log
  logback:
    rollingpolicy:
      max-file-size: 1MB

